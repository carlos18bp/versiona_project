[pytest]
DJANGO_SETTINGS_MODULE = versiona_project.settings
python_files = test_*.py tests.py
testpaths =
    accounts/tests
    core/tests
norecursedirs =
    venv
addopts = -ra --cov=accounts --cov=core --cov=versiona_project --cov-report=
